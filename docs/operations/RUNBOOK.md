# Operations — Runbook

See `ops/RUNBOOK.md` for the primary operations runbook covering:

- Quick reference (URLs, credentials)
- On-call rotation
- Severity levels
- Common procedures (verify health, restart Edge Functions, force redeploy, rollback, PITR, secret rotation)
- Escalation contacts

This file supplements the existing `ops/` runbook with procedures specific to post-launch operations.
